.DS_Store
xcuserdata/
DerivedData/
.build/
*.xcodeproj
*.log
build/
MonoriCore/build/
.recall/

# secrets
.env
.env.*
!.env.example

# vendor docs
docs/superpowers/

# Claude Code local state (team settings tracked at .claude/ root)
.claude/worktrees/
.claude/settings.local.json
.claude/settings.json.bak.*

# Generated knowledge-graph artifacts — regenerable derived caches.
# Kept local (not tracked) to avoid merge conflicts on feature branches.
# Regenerate with `graphify update .` / `/understand` (autoUpdate runs on commit).
graphify-out/
.understand-anything/
